node_modules

# Variables de entorno locales (cada dev tiene su .env; no versionar)
.env
.env.local
.env.*.local
server/.env
FRONT/.env.local

# Subidas y temporales del servidor (no versionar)
server/uploads/

# Datos catastrales pesados (XTF suele superar el límite de GitHub por archivo)
*.xtf

# Logs
*.log
